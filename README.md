# torch_camera_design

`torch_camera_design` は、ディレクトリ名と同名の Python パッケージ（src レイアウト）です。

## 開発

- 依存関係のインストール:
  ```bash
  pip install -e .[dev]
  ```
- テスト実行:
  ```bash
  pytest -q
  ```

## ライセンス

MIT
